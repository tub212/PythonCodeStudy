"""EXP"""
def exp(name, point):
    """BOOBOBOBOBO"""
    import math
    expcount = 0
    expsum = point/50.0
    calexp = (-5 + math.sqrt(abs(25 - 4*(expsum-4)))*0.5
    for i in xrange(int(calexp)+1):
        summa = (i**2)+5*(i)+4
        sumaa = summa*50
        count += summaa
    print count
exp(raw_input(), input())
