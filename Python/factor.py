"""factorial-ial-ial"""
###vCom
def factor(number, fac):
    """Fatorial"""
    numbersum = number
    while number > 0:
        number -= len(fac)
        print number
            
    print numbersum
factor(input(), raw_input())
