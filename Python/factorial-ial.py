"""Factorial_ial_ial"""
####Commnet
def factor(number, fac):
    """Find Factorial sum"""
    numbersum = number
    for i in reversed(xrange(1, number, len(fac)):
    if i <= 0:
            pass
        else:
            count = i
            numbersum = numbersum * count
    print numbersum
factor(abs(input()), raw_input())
